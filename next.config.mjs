/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "source.unsplash.com",
            "images.pexels.com",
            "cdn.pixabay.com",
            "via.placeholder.com"
        ],
    },
};

export default nextConfig;
